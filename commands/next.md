---
description: Show the one next staff-engineer step for the current concern.
---

Run `node .staff-engineer/cli.mjs next --json`. Do exactly the step it names: run its command, read only the skills it lists, and when `waitFor` is `operator`, send your message and stop. Never ask the operator to run anything.
