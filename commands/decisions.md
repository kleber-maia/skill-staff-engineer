---
description: Show earlier operator decisions relevant to a topic or files.
---

Run `node .staff-engineer/cli.mjs decisions --for "$ARGUMENTS" --json` (omit `--for` to list all active decisions). Summarize them for the operator in plain language. Treat them as settled unless the operator changes their mind.
