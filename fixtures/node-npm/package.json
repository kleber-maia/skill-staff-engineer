{
  "name": "fixture-node",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "vite build",
    "dev": "vite"
  },
  "devDependencies": { "vite": "^5.0.0", "vitest": "^1.0.0", "typescript": "^5.0.0", "eslint": "^9.0.0", "prettier": "^3.0.0" }
}
